version: '2'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - web.env
    volumes:
      - .:/django
    ports:
      - "8001:8000"
    tty: true
    stdin_open: true
